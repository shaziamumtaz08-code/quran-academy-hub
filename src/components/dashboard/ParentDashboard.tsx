// Re-export the real Parent Portal so any entry point (e.g. /dashboard switch)
// renders the same rich parent experience as the /parent route.
export { default as ParentDashboard } from '@/pages/ParentDashboard';
